"""
Apply page — executes approved renames and shows the audit log.

Calls Renamer with the approved suggestion list, supports dry-run preview,
and displays a color-coded result table (success / skipped / error) once
the operation completes.
"""
