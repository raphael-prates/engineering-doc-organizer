"""
Scan page — runs the Scanner and displays results.

Triggers a recursive filesystem scan on the selected folder, shows a progress
indicator, and presents a summary table of discovered files grouped by pattern
(standard / unknown / trash).
"""
