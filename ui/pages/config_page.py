"""
Config page — project and standard settings.

Lets the user select a naming standard (OTTS-016 or custom), configure
company/terminal codes, and save preferences that persist across sessions.
"""
